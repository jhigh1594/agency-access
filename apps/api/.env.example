# Environment
NODE_ENV=development

# Server
PORT=3001

# Backend API URL (for OAuth callbacks - use your actual backend URL in production)
# In development, defaults to http://localhost:3001
# In production, set to your deployed backend URL (e.g., https://api.example.com)
API_URL=http://localhost:3001

# Database (Neon PostgreSQL)
DATABASE_URL=postgresql://user:password@host/database

# Frontend URL
FRONTEND_URL=http://localhost:3000

# Clerk Authentication
CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...

# Infisical (for token storage)
INFISICAL_CLIENT_ID=
INFISICAL_CLIENT_SECRET=
INFISICAL_PROJECT_ID=
INFISICAL_ENVIRONMENT=dev

# Redis (Upstash or local)
REDIS_URL=redis://localhost:6379

# Meta (Facebook) OAuth
META_APP_ID=your-meta-app-id
META_APP_SECRET=your-meta-app-secret

# Google OAuth (unified - covers all Google products: Ads, Analytics, Business, Tag Manager, Search Console, Merchant Center)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
# Optional: Google Ads Developer Token (required for Google Ads API access)
GOOGLE_ADS_DEVELOPER_TOKEN=your-google-ads-developer-token

# Logging
LOG_LEVEL=info
