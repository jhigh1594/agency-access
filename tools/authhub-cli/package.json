{
  "name": "@agency-platform/authhub-cli",
  "version": "0.1.0",
  "private": true,
  "description": "CLI for AuthHub — agent-friendly command interface",
  "type": "module",
  "bin": {
    "authhub": "./dist/bin/authhub.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "@agency-platform/shared": "*",
    "@types/node": "^20.0.0",
    "typescript": "^5.7.2",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
